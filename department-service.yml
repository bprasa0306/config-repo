server:
  port: 8081

  spring:
    datasource:
      url: jdbc:mysql://mysql:3306/deptdb
      username: root
      password: admrootin
    jpa:
      hibernate:
        ddl-auto: update
        show-sql: true

  eureka:
    client:
      service-url:
        defaultZone: http://localhost:8761/eureka